@model IEnumerable<CalendarMvc.Models.OutlookToken>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Token)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Email)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Token)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Email)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.OutlookTokenID }) |
            @Html.ActionLink("Details", "Details", new { id=item.OutlookTokenID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.OutlookTokenID })
        </td>
    </tr>
}

</table>
